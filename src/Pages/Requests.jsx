import { useEffect, useState } from "react";
import { getData, saveData } from "../data";

export default function Requests() { const [requests,setRequests]=useState([]); useEffect(()=>setRequests(getData("libraryRequests")),[]); const update=(id,status)=>{const u=requests.map(r=>r.id===id?{...r,status}:r);setRequests(u);saveData("libraryRequests",u);}; return <><div className="breadcrumb">Home <span>›</span> Requested Books</div><div className="page-title"><h1>Requested Books</h1></div><section className="panel"><div className="table-wrap"><table><thead><tr><th>Student</th><th>Book</th><th>Date</th><th>Status</th><th>Action</th></tr></thead><tbody>{requests.map(r=><tr key={r.id}><td>{r.student}</td><td>{r.book}</td><td>{r.date}</td><td>{r.status}</td><td>{r.status === "Pending" && <><button className="small-btn" onClick={()=>update(r.id,"Approved")}>Approve</button><button className="small-btn danger-btn" onClick={()=>update(r.id,"Rejected")}>Reject</button></>}</td></tr>)}</tbody></table></div></section></>; }
